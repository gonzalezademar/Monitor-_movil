name: Deploy Radar Familiar a GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm install

      - name: Compilar la aplicación
        run: npm run build
        env:
          VITE_BASE_URL: /Monitor-_movil/
          VITE_SUPABASE_URL: https://azahxvyukjoayjazguik.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_kPA1kTqNgDi0amrtgWvDlg_nU7qLH94

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v4

      - name: Subir archivos compilados
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      - name: Publicar en GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
